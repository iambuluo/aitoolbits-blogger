# 2026-06-14 Anti-Duplicate Fix Deployment

## Problem
- `published_urls.json` 完全为空，防重复机制形同虚设
- `github_trending.py` 的 `published_repos.json` 也不存在
- 每次生成文章都用随机 star 数，导致 ECC 等文章反复出现
- DeepSeek 的 `get_random_topic()` 每次从 topics.py 选相同选题，但标题因 star 数差异而绕过检查

## Fix
1. 修改 `published_urls.py`：添加 title normalization（去除 star count 差异），改进 `_normalize_title()` 正则匹配
2. 修改 `generate_article.py`：在 `generate_article()` 和 `generate_article_from_topic()` 中都加入 pre-flight 重复检查
3. 修改 `publish_to_blogger.py`：发布成功后自动记录 URL 到 `published_urls.json`
4. 新增 `sync_existing_articles.py`：通过 Blogger API 拉取已有文章填充数据库
5. 新增 `check_gsc_indexing.py` + `gsc-monitor.yml`：GSC 每3天检查一次，Server酱推送到微信
6. 禁用 `publish-trending.yml` → `_disabled-publish-trending.yml`
7. Commit: b68ed6d，已 push to GitHub

## 2026-06-16 Update (Completion)
8. **Populated published_urls.json**: Fetched 69 existing articles via Blogger API (commits c65895c, ee5ed02)
9. **Found 16 duplicate groups**: ECC x3, Jasper x3, ChatGPT x2, etc. — confirmed the bug
10. **Fixed .gitignore**: Removed `*.json` blanket exclusion, allowed `data/published_urls.json`
11. **Created sync-existing-articles.yml**: Workflow for future re-syncs via workflow_dispatch
12. **Verified duplicate detection**: ECC star count variations correctly detected as duplicates
13. **Root cause**: Anti-duplicate system was deployed (commit b68ed6d) but `published_urls.json` was empty
    because sync was never actually run. Now fully populated with 69 articles.
14. **Remaining**: GSC monitor workflow needs `SERVER_CHAN_KEY` secret to be configured in GitHub repo settings.
