# 2026-05-29 工作日志

## 排查并修复随机延迟导致的 workflow 超时取消 bug

### 问题
- 用户反映今天没有自动更新
- 诊断：Run #12（01:00 BJ触发）被 cancelled，Run #13（12:45 BJ触发）in_progress 中
- 根因：workflow timeout-minutes=15，但随机延迟最高 2h（7200秒），导致 run 在 delay 阶段被强制取消

### 修复
- 将  改为 
- 通过 GitHub Contents API 直接推送（git push 因网络问题不通，用 Python urllib 绕过）
- 推送成功，commit: 709a8fb

### 当前状态
- Run #13（12:45触发）仍用旧配置，大概率会在 13:00 左右再次 cancel（旧超时 15min）
- Run #14（今晚约 21:08 BJ触发）起，将使用新的 180min timeout，随机延迟后正常发布
- git push 走 git 协议有问题（走了系统代理127.0.0.1），改用 GitHub Contents API 推送

### 踩坑
- 随机延迟和 timeout-minutes 必须匹配：delay 最大值必须 < timeout
- git push 在此机器上不通时，可用 Python urllib + GitHub Contents API 推送单文件
